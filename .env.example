# Optional production configuration
CUSTOMERIQ_ADMIN_EMAIL=admin123@gmail.com
# SHA-256 hash of the admin password. Generate your own hash for deployment.
CUSTOMERIQ_ADMIN_PASSWORD_HASH=240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9

# Optional PostgreSQL database. Leave unset to use the local SQLite database.
# DATABASE_URL=postgresql://username:password@localhost:5432/customeriq

# Optional LLM-backed grounded assistant. Without this key, the offline verified-answer fallback is used.
# OPENAI_API_KEY=your_api_key_here
# OPENAI_MODEL=gpt-5.6
